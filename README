Bases turtle startup:
```
cd /
shell.run("delete elements.lua")
shell.run("wget https://raw.githubusercontent.com/svencan/elements/master/bases.lua)
```

Then save, exit, and type `bases` to start.

Computer startup:
```
cd /
shell.run("delete elements.lua")
shell.run("delete gib.lua")
shell.run("wget https://raw.githubusercontent.com/svencan/elements/master/elements.lua)
shell.run("wget https://raw.githubusercontent.com/svencan/elements/master/gib.lua)
```

Then save, exit, use with "gib [element-number] [amount]" or simply "gib".