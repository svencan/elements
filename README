Request computer setup:
```
cd /
edit startup
shell.run("wget https://raw.githubusercontent.com/svencan/elements/master/gib.lua startup)
```

Then save, exit, and type `reboot`.

Turtle setup:
```
cd /
edit startup
shell.run("wget https://raw.githubusercontent.com/svencan/elements/master/hol.lua startup)
```

Then save, exit, and type `reboot`.